% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{summary.bbni}
\alias{summary.bbni}
\title{Posterior Summary of a BBNI MCMC Object}
\usage{
\method{summary}{bbni}(object, threshold = 0.5, n_top = 5, ...)
}
\arguments{
\item{object}{A \code{bbni} object returned by \code{run_bbni()}.}

\item{threshold}{Numeric. Posterior probability threshold for counting
strong edges.}

\item{n_top}{Integer. Number of highest-posterior edges to display and
include in \code{top_edges}.}

\item{...}{Additional arguments (ignored).}
}
\value{
A list with components \code{num_nodes}, \code{num_update}, \code{burn_in},
\code{final_log_posterior}, \code{n_strong_edges}, and \code{top_edges}
(data frame with columns \code{child}, \code{parent}, and \code{posterior}).
}
\description{
Prints a statistical summary of the MCMC run and returns a structured list
of posterior quantities for further usage in downstream code.
}
\examples{
\dontrun{
set.seed(123)
net <- GenerateNetwork(5)
data <- GenerateSample(net, 100)
res <- run_bbni(data, num_update = 50)
summary(res, threshold = 0.6, n_top = 3)
}
}
